import { connect } from 'react-redux'
import RadioGroup from './units'

const mapStateToProps = () => ({
})

const mapDispatchToProps = () => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(RadioGroup)
