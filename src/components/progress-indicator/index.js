import { connect } from 'react-redux'
import ProgressIndicator from './units'

const mapStateToProps = () => ({
})

const mapDispatchToProps = () => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(ProgressIndicator)
